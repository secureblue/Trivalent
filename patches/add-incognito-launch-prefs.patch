Copyright 2024-2025 The Trivalent Authors

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software distributed under the License is
distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and limitations under the License.
---
diff --git a/chrome/browser/prefs/incognito_mode_prefs.cc b/chrome/browser/prefs/incognito_mode_prefs.cc
index ce5660ca65a4f..4cf8a3dbb1eaa 100644
--- a/chrome/browser/prefs/incognito_mode_prefs.cc
+++ b/chrome/browser/prefs/incognito_mode_prefs.cc
@@ -162,6 +162,9 @@ bool IncognitoModePrefs::ShouldLaunchIncognitoInternal(
   }
   bool should_use_incognito =
       forced_by_switch ||
+      (prefs->GetBoolean(prefs::kIncognitoLaunch) && !for_subsequent_browsers &&
+       !command_line.HasSwitch(switches::kAppId) &&
+       !command_line.HasSwitch(switches::kApp)) ||
       GetAvailabilityInternal(prefs, DONT_CHECK_PARENTAL_CONTROLS) ==
           IncognitoModeAvailability::kForced;
   return should_use_incognito &&
diff --git a/chrome/browser/profiles/profile.cc b/chrome/browser/profiles/profile.cc
index a490fefce8c79..1b071f5c7c571 100644
--- a/chrome/browser/profiles/profile.cc
+++ b/chrome/browser/profiles/profile.cc
@@ -316,6 +316,8 @@ const char Profile::kProfileKey[] = "__PROFILE__";
 
 // static
 void Profile::RegisterProfilePrefs(user_prefs::PrefRegistrySyncable* registry) {
+  registry->RegisterBooleanPref(prefs::kIncognitoLaunch, false);
+  registry->RegisterBooleanPref(prefs::kIncognitoStartup, true);
   registry->RegisterBooleanPref(
       prefs::kSearchSuggestEnabled,
       true,
diff --git a/chrome/browser/ui/startup/startup_browser_creator.cc b/chrome/browser/ui/startup/startup_browser_creator.cc
index 8c99b3663e..757f2704e0 100644
--- a/chrome/browser/ui/startup/startup_browser_creator.cc
+++ b/chrome/browser/ui/startup/startup_browser_creator.cc
@@ -719,6 +719,13 @@ ProfileSetupResult SetupProfileAndIncognito(
   ProfileSetupResult result;
   result.silent_launch = false;

+  if (profile_info.profile->GetPrefs()->GetBoolean(prefs::kIncognitoStartup))
+    incognito_startup = IncognitoModePrefs::ShouldLaunchIncognito(
+                            command_line, profile_info.profile->GetPrefs());
+  else
+    incognito_startup = IncognitoModePrefs::ShouldOpenSubsequentBrowsersInIncognito(
+                            command_line, profile_info.profile->GetPrefs());
+
   result.should_launch_incognito =
       // Note: kIncognito and some related flags disable profile picker startups
       // via `ShouldShowProfilePickerAtProcessLaunch()`, so we can use it as
@@ -726,8 +733,7 @@ ProfileSetupResult SetupProfileAndIncognito(
       // TODO(crbug.com/40819749): Refactor command line processing logic
       // to validate the flag sets and reliably determine the startup mode.
       profile_info.mode != StartupProfileMode::kProfilePicker &&
-      IncognitoModePrefs::ShouldLaunchIncognito(
-          command_line, profile_info.profile->GetPrefs());
+      incognito_startup;

   result.can_use_profile =
diff --git a/chrome/common/pref_names.h b/chrome/common/pref_names.h
index 32141931a58d5..93cf4c2a33d54 100644
--- a/chrome/common/pref_names.h
+++ b/chrome/common/pref_names.h
@@ -28,6 +28,12 @@ namespace prefs {
 // *************** PROFILE PREFS ***************
 // These are attached to the user profile
 
+// Launch external links in Incognito
+inline constexpr char kIncognitoLaunch[] = "incognito.launch_mode";
+
+// Start sessions in Incognito
+inline constexpr char kIncognitoStartup[] = "incognito.startup_mode";
+
 // A string property indicating whether default apps should be installed
 // in this profile.  Use the value "install" to enable defaults apps, or
 // "noinstall" to disable them.  This property is usually set in the
