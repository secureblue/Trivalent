Copyright 2024-2026 The Trivalent Authors

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software distributed under the License is
distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and limitations under the License.
---
diff --git a/chrome/app/chrome_command_ids.h b/chrome/app/chrome_command_ids.h
index f2b923756b..885e1c02c6 100644
--- a/chrome/app/chrome_command_ids.h
+++ b/chrome/app/chrome_command_ids.h
@@ -163,6 +163,8 @@
 #define IDC_GROUP_TARGET_TAB            35052
 #define IDC_DUPLICATE_TARGET_TAB        35053

+#define IDC_WEBSITE_DARKMODE            35500
+
 // Clipboard commands
 #define IDC_CUT                         36000
 #define IDC_COPY                        36001
diff --git a/chrome/browser/ui/actions/chrome_action_id.h b/chrome/browser/ui/actions/chrome_action_id.h
index 9da2b68be3..f7cb7b9104 100644
--- a/chrome/browser/ui/actions/chrome_action_id.h
+++ b/chrome/browser/ui/actions/chrome_action_id.h
@@ -579,6 +579,7 @@
   E(kActionShowDownloads, IDC_SHOW_DOWNLOADS) \
   E(kActionClearBrowsingData, IDC_CLEAR_BROWSING_DATA) \
   E(kActionPrint, IDC_PRINT) \
+  E(kActionWebsiteDarkmode, IDC_WEBSITE_DARKMODE) \
   E(kActionShowTranslate, IDC_SHOW_TRANSLATE) \
   E(kActionSendTabToSelf, IDC_SEND_TAB_TO_SELF) \
   E(kActionQrCodeGenerator, IDC_QRCODE_GENERATOR) \
diff --git a/chrome/browser/ui/browser_actions.cc b/chrome/browser/ui/browser_actions.cc
index bf32395f9b..58ac707068 100644
--- a/chrome/browser/ui/browser_actions.cc
+++ b/chrome/browser/ui/browser_actions.cc
@@ -687,6 +687,16 @@ void BrowserActions::InitializeBrowserActions() {
           .SetEnabled(chrome::CanPrint(bwi))
           .Build());

+  root_action_item_->AddChild(
+      ChromeMenuAction(
+          base::BindRepeating(
+              [](BrowserWindowInterface* bwi, actions::ActionItem* item,
+                 actions::ActionInvocationContext context) {
+                chrome::ToggleWebsiteDarkmode(bwi->GetProfile()->GetPrefs());
+              }, bwi),
+          kActionWebsiteDarkmode, IDS_WEBSITE_DARK_MODE, IDS_WEBSITE_DARK_MODE,
+          kWebsiteDarkmodeIcon).Build());
+
   const bool is_incognito = profile_->IsIncognitoProfile();
   root_action_item_->AddChild(
       ChromeMenuAction(base::BindRepeating(
diff --git a/chrome/browser/ui/browser_command_controller.cc b/chrome/browser/ui/browser_command_controller.cc
index d637eecb9e..621b51f79c 100644
--- a/chrome/browser/ui/browser_command_controller.cc
+++ b/chrome/browser/ui/browser_command_controller.cc
@@ -745,6 +745,10 @@ bool BrowserCommandController::ExecuteCommandWithDisposition(
       Print(browser_);
       break;

+    case IDC_WEBSITE_DARKMODE:
+      ToggleWebsiteDarkmode(browser_->profile()->GetPrefs());
+      break;
+
 #if BUILDFLAG(ENABLE_PRINTING)
     case IDC_BASIC_PRINT:
       base::RecordAction(base::UserMetricsAction("Accel_Advanced_Print"));
@@ -1687,6 +1691,7 @@ void BrowserCommandController::InitCommandState() {
   command_updater_.UpdateCommandEnabled(
       IDC_SHOW_TABS_FROM_OTHER_DEVICES_SIDE_PANEL,
       TabsFromOtherDevicesSidePanelCoordinator::IsSupported(profile()));
+  command_updater_.UpdateCommandEnabled(IDC_WEBSITE_DARKMODE, true);
   command_updater_.UpdateCommandEnabled(IDC_SHOW_DOWNLOADS, true);
   command_updater_.UpdateCommandEnabled(IDC_SHOW_COMMENTS_SIDE_PANEL, true);
   command_updater_.UpdateCommandEnabled(IDC_FIND_AND_EDIT_MENU, true);
diff --git a/chrome/browser/ui/browser_commands.cc b/chrome/browser/ui/browser_commands.cc
index cd7f650386..261e7e0d53 100644
--- a/chrome/browser/ui/browser_commands.cc
+++ b/chrome/browser/ui/browser_commands.cc
@@ -2371,6 +2371,11 @@ void OpenFeedbackDialog(BrowserWindowInterface* browser,
                            category_tag, std::string() /* extra_diagnostics */);
 }
 
+void ToggleWebsiteDarkmode(PrefService* pref_service) {
+  bool pref_enabled = pref_service->GetBoolean(prefs::kWebKitForceDarkModeEnabled);
+  pref_service->SetBoolean(prefs::kWebKitForceDarkModeEnabled, !pref_enabled);
+}
+
 #if BUILDFLAG(GOOGLE_CHROME_BRANDING)
 void OpenReportUnsafeSiteDialog(BrowserWindowInterface* browser) {
   base::RecordAction(UserMetricsAction("ReportUnsafeSite"));
diff --git a/chrome/browser/ui/browser_commands.h b/chrome/browser/ui/browser_commands.h
index 9a4b59415d..06f30410b0 100644
--- a/chrome/browser/ui/browser_commands.h
+++ b/chrome/browser/ui/browser_commands.h
@@ -28,6 +28,7 @@ class BrowserWindowInterface;
 class CommandObserver;
 class GURL;
 class Profile;
+class PrefService;
 enum class DevToolsOpenedByAction;
 
 namespace content {
@@ -251,6 +252,7 @@ void SharingHub(BrowserWindowInterface* browser);
 void ScreenshotCapture(BrowserWindowInterface* browser);
 void SavePage(BrowserWindowInterface* browser);
 bool CanSavePage(const BrowserWindowInterface* browser);
+void ToggleWebsiteDarkmode(PrefService* pref_service);
 void Print(BrowserWindowInterface* browser);
 bool CanPrint(BrowserWindowInterface* browser);
 #if BUILDFLAG(ENABLE_PRINTING)
diff --git a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_toolbar/customize_toolbar.mojom b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_toolbar/customize_toolbar.mojom
index dc2fed7651..d1956d42ce 100644
--- a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_toolbar/customize_toolbar.mojom
+++ b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_toolbar/customize_toolbar.mojom
@@ -8,6 +8,7 @@ import "url/mojom/url.mojom";

 // Unique identifiers for actions that can be pinned to the toolbar.
 enum ActionId {
+  kWebsiteDarkmode,
   kShowBookmarks,
   kShowHistoryCluster,
   kShowReadAnything,
diff --git a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_toolbar/customize_toolbar_handler.cc b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_toolbar/customize_toolbar_handler.cc
index c442fa1941..db6ef82436 100644
--- a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_toolbar/customize_toolbar_handler.cc
+++ b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_toolbar/customize_toolbar_handler.cc
@@ -40,6 +40,8 @@ namespace {
 std::optional<side_panel::customize_chrome::mojom::ActionId>
 MojoActionForChromeAction(actions::ActionId action_id) {
   switch (action_id) {
+    case kActionWebsiteDarkmode:
+      return side_panel::customize_chrome::mojom::ActionId::kWebsiteDarkmode;
     case kActionSidePanelShowBookmarks:
       return side_panel::customize_chrome::mojom::ActionId::kShowBookmarks;
     case kActionSidePanelShowHistoryCluster:
@@ -102,6 +104,8 @@ MojoActionForChromeAction(actions::ActionId action_id) {
 std::optional<actions::ActionId> ChromeActionForMojoAction(
     side_panel::customize_chrome::mojom::ActionId action_id) {
   switch (action_id) {
+    case side_panel::customize_chrome::mojom::ActionId::kWebsiteDarkmode:
+      return kActionWebsiteDarkmode;
     case side_panel::customize_chrome::mojom::ActionId::kShowBookmarks:
       return kActionSidePanelShowBookmarks;
     case side_panel::customize_chrome::mojom::ActionId::kShowHistoryCluster:
@@ -350,6 +354,8 @@ void CustomizeToolbarHandler::ListActions(ListActionsCallback callback) {
              side_panel::customize_chrome::mojom::CategoryId::kYourChrome);
   add_action(kActionClearBrowsingData,
              side_panel::customize_chrome::mojom::CategoryId::kYourChrome);
+  add_action(kActionWebsiteDarkmode,
+             side_panel::customize_chrome::mojom::CategoryId::kYourChrome);
 
   if (!base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton) &&
       features::HasTabSearchToolbarButton()) {
diff --git a/chrome/app/vector_icons/BUILD.gn b/chrome/app/vector_icons/BUILD.gn
index 7537afaf13..9c17b4466e 100644
--- a/chrome/app/vector_icons/BUILD.gn
+++ b/chrome/app/vector_icons/BUILD.gn
@@ -244,6 +244,7 @@ aggregate_vector_icons("chrome_vector_icons") {
     "webauthn/windows_hello_color.icon",
     "webid/webid_arrow.icon",
     "webid/webid_globe.icon",
+    "website_darkmode.icon",
     "zoom_in.icon",
     "zoom_in_map.icon",
     "zoom_minus.icon",
diff --git a/chrome/app/vector_icons/website_darkmode.icon b/chrome/app/vector_icons/website_darkmode.icon
--- /dev/null
+++ b/chrome/app/vector_icons/website_darkmode.icon
@@ -0,0 +1,53 @@
+// Copyright 2026 The Trivalent Authors
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software distributed under the License is
+// distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and limitations under the License.
+
+CANVAS_DIMENSIONS, 24,
+FILL_RULE_NONZERO,
+MOVE_TO, 21, 3,
+H_LINE_TO, 3,
+R_CUBIC_TO, -1.1f, 0, -2, 0.9f, -2, 2,
+R_V_LINE_TO, 14,
+R_CUBIC_TO, 0, 1.1f, 0.9f, 2, 2, 2,
+R_H_LINE_TO, 18,
+R_CUBIC_TO, 1.1f, 0, 2, -0.9f, 2, -2,
+V_LINE_TO, 5,
+R_CUBIC_TO, 0, -1.1f, -0.9f, -2, -2, -2,
+CLOSE,
+R_MOVE_TO, 0, 16.01f,
+H_LINE_TO, 3,
+V_LINE_TO, 4.99f,
+R_H_LINE_TO, 18,
+R_V_LINE_TO, 14.02f,
+CLOSE,
+MOVE_TO, 8, 16,
+R_H_LINE_TO, 2.5f,
+R_LINE_TO, 1.5f, 1.5f,
+R_LINE_TO, 1.5f, -1.5f,
+H_LINE_TO, 16,
+R_V_LINE_TO, -2.5f,
+R_LINE_TO, 1.5f, -1.5f,
+R_LINE_TO, -1.5f, -1.5f,
+V_LINE_TO, 8,
+R_H_LINE_TO, -2.5f,
+LINE_TO, 12, 6.5f,
+LINE_TO, 10.5f, 8,
+H_LINE_TO, 8,
+R_V_LINE_TO, 2.5f,
+LINE_TO, 6.5f, 12,
+LINE_TO, 8, 13.5f,
+V_LINE_TO, 16,
+CLOSE,
+R_MOVE_TO, 4, -7,
+R_CUBIC_TO, 1.66f, 0, 3, 1.34f, 3, 3,
+R_CUBIC_TO, 0, 1.66f, -1.34f, 3, -3, 3,
+V_LINE_TO, 9,
+CLOSE
